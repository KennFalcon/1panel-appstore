additionalProperties:
  formFields:
    - default: 5601
      edit: true
      envKey: PANEL_APP_PORT_HTTP
      labelEn: Port
      labelZh: 端口
      required: true
      rule: paramPort
      type: number
    - default: "http://127.0.0.1:9200"
      edit: true
      envKey: elasticsearch_hosts
      labelZh: Elasticsearch 地址
      labelEn: Elasticsearch address
      required: true
      type: text
    - default: "elastic"
      edit: true
      envKey: elasticsearch_username
      labelZh: Elasticsearch用户名
      labelEn: Elasticsearch username
      required: false
      type: text
    - default: "elastic"
      edit: true
      envKey: elasticsearch_password
      labelZh: Elasticsearch密码
      labelEn: Elasticsearch password
      required: false
      type: password